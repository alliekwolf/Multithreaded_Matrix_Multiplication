/**
 * 
 * @author Brian Steele
 * @author Cole Walsh
 * @author Allie Wolf
 *
 */
public class SharedBuffer {
	
	private int[] workItemsBuffer;
	private int maxBufferSize;
	private int fullBufferCount;
	private int emptyBufferCount;
	
	
	
	 public synchronized void put(WorkItem item) {
		return ;
	}
	 
	/* public synchronized int get() {
			while (____ == true/false) {
				try {
					wait();
				} catch (InterruptedException e) {}
			}
			______ = false;
			notifyAll();
			return ;
		}
	 */
	 

	

	private boolean checkinBufferSize(int maxBufferSize) {
		return true;
	}
	
	private void incrementFullBufferCount() {
		
	}
	
	private void incrementEmptyBufferCount() {
		
	}
	
	public int getFullBufferCount() {
		return 0;
	}
	
	public int getEmptyBufferCount() {
		return 0;
	}

}
